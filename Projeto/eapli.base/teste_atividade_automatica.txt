BEGIN
    XML $file -> "exemplo.xml"
        ELEMENTO $specificProduct -> FIND [prod,$file] where (id,5)
    LMX

    TEXTO $price -> $specificProduct(preco)
  //NUMERO $price -> $specificProduct(price)
  //TEXTO $status
/*
    se $price >0
    entao
        $status-> 2
    senao
        $status-> 2
    es
*/
    TEXTO $destinatario-> "guili@isep.ipp.pt"
    TEXTO $assunto -> "Teste 1"

    se $price<5 entao
        TEXTO $corpo-> "O preço é menor que 5 : " + $price + "$."
    senao
        TEXTO $corpo-> "O preço é maior que 5 : " + $price + "$."
    es

    //TEXTO assunto -> 4 > O produto : {{$nome}} está agora em promoção com o status {{$status}}
    ENVIAR_EMAIL($destinatario,$assunto,$corpo)

    $nome -> "Restauro Porta"
    ATUALIZAR(service,serviceid,2) -> (servicetitle,$nome)

END